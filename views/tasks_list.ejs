<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>授業課題管理システム</title>
</head>
<body>
    <h1>課題一覧</h1>
    <a href="/tasks/create">新規追加</a>
    <hr>
    <table border="1">
        <tr>
            <th>ID</th>
            <th>科目名</th>
            <th>内容</th>
            <th>期限</th>
            <th>時間(分)</th>
            <th>操作</th>
        </tr>
        <% for (let row of data) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.subject %></td>
            <td><%= row.title %></td>
            <td><%= row.deadline %></td>
            <td><%= row.time %></td>
            <td><a href="/tasks/<%= row.id %>">詳細</a></td>
        </tr>
        <% } %>
    </table>
</body>
</html>